// Custom layout for products-showcase page - No header/footer
export default function ProductShowcaseLayout({ children }) {
    return (
        <div className="products-showcase-layout">
            {children}
        </div>
    );
}
